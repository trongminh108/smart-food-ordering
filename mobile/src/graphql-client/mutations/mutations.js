import * as orders from './orders';
export const { addOrder, updateOrder, removeOrder } = orders;

import * as order_details from './order_details';
export const { addOrderDetails, updateOrderDetails, removeOrderDetails } =
    order_details;
